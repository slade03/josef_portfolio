/**
 * Skills — section header copy only.
 *
 * The skills themselves are Josef's REAL ones, in `src/content/experience.ts`.
 * They already arrive grouped with counts, which is exactly the shape this
 * section renders, so there is nothing to transform and nothing to invent.
 *
 * The four placeholder tabs that used to live here (Research / Design / Build /
 * Systems) are gone with the pinned tab layout they existed to fill.
 */

export const skills = {
  title: "Skills",
  lede: "Design and build are one job here, not a handoff. The list below is what that covers in practice.",
} as const;
